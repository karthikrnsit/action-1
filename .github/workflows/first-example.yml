name: verify
on: push
jobs:
  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout 
        uses: actions/checkout@v4
      - name: Executing shell script 
        run: |
         chmod +x sampleScript.sh
         ./sampleScript.sh
            

